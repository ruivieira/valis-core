window.SIDEBAR_ITEMS = {"fn":[["get_venv_info","Returns a `VirtualEnv` struct for the specified `PathBuf` path. Assumes that the virtualenvs are located in `~/.virtualenvs` and that the virtualenv name is the same as the project name."],["rebuild",""],["status","Prints the status of the virtualenv for the current project at `path`. Assumes that the virtualenvs are located in `~/.virtualenvs` and that the virtualenv name is the same as the project name."]],"struct":[["VirtualEnv",""]]};